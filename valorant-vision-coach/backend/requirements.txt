# Core runtime — always required. The app boots and runs end to end with just
# these (using the mock detector). Heavy ML deps live in requirements-ml.txt.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
python-multipart==0.0.20
numpy==1.26.4
opencv-python-headless==4.10.0.84
